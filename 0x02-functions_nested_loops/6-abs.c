#include "main.h"

/**
* _abs - computes absolute value of a integer
* @n: parameter
*
* Return: absolute value of a number
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}

}
