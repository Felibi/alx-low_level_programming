#include "main.h"

/**
 * print_square - print square
 * @size: size of square
 *
 * Return: nothing
 */


void print_square(int size)
{
	int k, b;

	if (size <= 0)
		_putchar('\n');
	else
	{
		for (k = 0; k < size; K++)
		{
			for (b = 0; b < (size); b++)
			{
				_putchar('35');
			}
			_putchar('\n');
		}
		_putchar('\n');
	}
}
